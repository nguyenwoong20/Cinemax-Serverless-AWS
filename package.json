{
  "name": "cinemax-serverless-aws",
  "version": "1.0.0",
  "description": "Serverless movie API on AWS for the Cinemax Flutter app (API Gateway + Lambda + DynamoDB + S3 + CloudWatch)",
  "scripts": {
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "bcryptjs": "^2.4.3",
    "google-auth-library": "^9.14.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.14"
  }
}
